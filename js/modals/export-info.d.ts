export declare class ExportInfo {
    choosed: string;
    options: Array<string>;
    constructor(choosed: string, options: Array<string>);
    static create(choosed: string, options: Array<string>): ExportInfo;
}
